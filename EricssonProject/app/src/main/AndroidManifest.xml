<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ezcolth.ericssonproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/an"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/an"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".profile" />
        <activity android:name=".Pop" />
        <activity android:name=".MainMenu" />
        <activity android:name=".Login" />
        <activity android:name=".Settings" />
        <activity android:name=".TermsAndConditions" />
        <activity android:name=".changePassword" />
        <activity android:name=".authentication" />
        <activity android:name=".MessageScreen1" />
        <activity android:name=".MessageScreen"></activity>
    </application>

</manifest>